# Copie para .env e preencha conforme necessário

# Chave da API Anthropic (Claude). Deixe vazio para modo demo/mock.
ANTHROPIC_API_KEY=sk-...

# Segredo para tokens JWT. Em dev há fallback; em produção use um valor seguro.
JWT_SECRET=seu-segredo-aqui
